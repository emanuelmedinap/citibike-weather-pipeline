# Enough to run the tests and the dashboard locally. No cloud SDKs.
# Python 3.12 or newer. Minimum versions, not pins, so current wheels resolve.
pandas>=2.2
pyarrow>=18
pytest>=8
streamlit>=1.39
plotly>=5.24
numpy>=2.1
